node_modules/
.next/
.vercel/
*.tsbuildinfo
.DS_Store
.env*.local

# Local SQLite read-through cache (warmed by scripts/fetch-ll84.ts; the demo
# falls back to committed JSON fixtures under test/fixtures/demo/).
data/cache/
