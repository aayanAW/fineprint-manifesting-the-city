{
  "name": "fineprint-manifesting-the-city",
  "version": "0.1.0",
  "private": true,
  "description": "Fineprint — Manifesting the City. Pivot of the NYC LL97 engine for a new hackathon. Engine carried over; front-end TBD after brainstorm.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vite-tsconfig-paths": "^6.1.1",
    "vitest": "^4.1.8"
  }
}
